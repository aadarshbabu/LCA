// import Valkey from "ioredis";
// const serviceUri = process.env.REDIS_URI;

// if (!serviceUri) {
//   throw new Error("REDIS_URI environment variable is not set.");
// }

// // Log the URI for debugging purposes (ensure this is safe in your environment)
// // console.log("Connecting to Redis with URI:", serviceUri);

// // Sanitize the URI if necessary
// const sanitizedUri = serviceUri.replace(/^"|"$/g, "");

// export const valkey = new Valkey(sanitizedUri);
